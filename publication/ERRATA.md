# ERRATA — реестр ошибок и что с каждой сделано

Ведётся по рукописи `CIRPJ-D-26-00824` (отклонена 2026-08-23), по коду
репозитория и по данным. Источник истины для чисел — `audit/RAW_AUDIT.md`,
собранный из сырых `.rpt` релиза `raw-data-v1`; там, где рукопись, `docs/` и
прежний handover расходятся с данными, прав `.rpt`.

Статусы:

| знак | значение |
|---|---|
| ✅ | закрыто в этом заходе (код/доки в репозитории) |
| 🔶 | код готов, требуется прогон обучения |
| 📝 | правка в `.tex`, за пределами репозитория (Overleaf, решение Дёмина) |
| ⛔ | неустранимо имеющимися средствами, лечится переформулировкой вклада |

## Сводка

| # | важность | что | статус |
|---|---|---|---|
| E-01 | 🔴 | свободная поверхность стоит не там: `R_EXTRAP_FREE_SURFACE = 1.5` | ✅ + 🔶 |
| E-02 | 🔴 | `∂/∂z` в тексте против редуцированной формы в коде | ✅ + 📝 |
| E-03 | 🟡 | фантомный уровень `μ = 0.002` | ✅ + 📝 |
| E-04 | 🔴 | `PLANE_ORDER` не согласован с коммитнутым препроцессингом | ✅ (диагноз) + 🔶 |
| E-05 | 🟡 | протокол обучения из §2.2.4 рукописи в коде отсутствует | ✅ + 📝 |
| E-06 | 🟡 | μ-дубликаты: 13.2 %, а не «~31 %» | ✅ + 📝 |
| E-07 | 🔴 | hold-out мерит интерполяцию, а не обобщение | ✅ (код) + 🔶 |
| E-08 | 🔴 | один прогон на модель; ранговый тест по наборам, а не по сидам | ✅ (код) + 🔶 |
| E-09 | 🔴 | FEM не валидирован экспериментально | ⛔ + 📝 |
| E-10 | 🔴 | рецензент не видит методической новизны | ⛔ + 📝 |
| E-11 | 🟡 | «~8 наборов с Q = 0.25» против фактических 275 | ✅ |
| E-12 | 🟡 | несбалансированность по `v` не оговорена | ✅ + 📝 |
| E-13 | 🔴 | библиография не собирается, в PDF нет ссылок | 📝 |
| E-14 | 🔴 | продублирован список компонент в Conclusions | 📝 |
| E-15 | 🟡 | аннотация рассинхронизирована с телом статьи | 📝 |
| E-16 | 🟢 | косметика `.tex` | 📝 |
| E-17 | 🟡 | `PLANE_ORDER_STRAIN` в `training/data_pipeline.py` против `docs/` | ✅ (описано) |

---

## E-01 🔴 Свободная поверхность стоит не там

**Как было.** В `models/pinn_model_torch.py`, `pinn_model_dde.py`,
`pinn_model_jax.py`, `vpinn_model.py` — константа
`R_EXTRAP_FREE_SURFACE = 1.5` и обоснование в докстринге: «данные охватывают
`r_norm ∈ [0,1]` = 25–75 % физического радиуса, истинная свободная поверхность
при `r_norm = 1.5`». Прежний handover повторяет это формулой
`r_phys = R·(0.25 + 0.5·r_norm)`.

**Что показали данные** (`audit/RAW_AUDIT.md`, §2, 2443 набора):

* окно 25–75 % — **осевое**, по координате `z`: медиана доли осевого размаха,
  попавшей в окно, = 0.4985;
* радиальный профиль покрывает **весь** радиус: медиана `r_min(окно)/R` = 0.000,
  медиана `r_max(окно)/R` = 0.996;
* traction-free выполняется именно в крайней точке профиля: средний `|σ_rr|`
  там 3.77 МПа, `|τ_rz|` 0.48 МПа против `|σ_θθ|` 114 и `|σ_zz|` 129 МПа.

Значит `r_norm = 1` — это и есть поверхность, а `r_norm = 1.5` — радиус `1.5 R`,
точка вне материала. ГУ налагалось там, где материала нет, а измерялось
(`evaluation/physics_audit.py::bc_violation` берёт последнюю точку профиля) при
`r_norm = 1`. Условие и метрика жили в разных точках.

**Последствие для результата.** Числа «нарушение BC 0.6–1.2 МПа у PINN против
12–20 МПа у MLP» — это измерение эффекта регуляризатора, наложенного вне
области определения задачи. Сам эффект (сеть, притянутая к нулю в 1.5 R, ближе
к нулю и в R) реален, но описан он в статье неверно, и в таком виде цитировать
его нельзя.

**Сделано.** `publication/code/physics_check.py`: `R_FREE_SURFACE = 1.0`;
`bc_violation()` отказывается экстраполировать за сетку профиля и объясняет,
почему. `docs/physics_equations.md` и `docs/data_format.md` исправлены.

**Осталось (🔶).** Переобучить все шесть моделей с `r_extrap = 1.0` и
пересчитать Fig. 11–12 рядом с базовой линией самих данных FEM (3.8 / 0.5 МПа).
Ожидание, которое надо проверить, а не декларировать: преимущество PINN по ГУ
сохранится, но уменьшится, потому что при `r_norm = 1` приор частично
дублирует данные.

## E-02 🔴 `∂/∂z` в тексте против редуцированной формы в коде

**Как было.** В рукописи (`eq:eq_r`, `eq:eq_z`) напечатана полная форма с
`∂τ_rz/∂z` и `∂σ_zz/∂z`. В коде (`_strong_eq_loss_torch` и аналоги) осевых
членов нет и быть не может: `z` не входит в `X = [Q, k, α, μ, v, r]`.
Reviewer #1 указал ровно на это.

**Численная оценка отброшенного** (`audit/RAW_AUDIT.md`, §6; поля внутри
осевого окна на сетке 20 × 20, медианы по 2443 наборам):

| уравнение | отброшенный член | отношение к сумме удержанных | сдвиг невязки |
|---|---|---|---|
| радиальное | `∂τ_rz/∂z` | **0.013** (75 % перцентиль 0.039) | 6 % |
| осевое | `∂σ_zz/∂z` | **0.68** (75 % перцентиль 0.86) | 32 % |

**Вывод, который надо написать в статье.** Он не один, а два, и они разные:

1. Для радиального уравнения редукция **обоснована**: в зоне установившейся
   деформации поля по `z` почти однородны, отброшенный член на два порядка
   меньше удержанных. Это можно писать, и теперь есть чем подкрепить.
2. Для осевого уравнения редукция **не обоснована**: отброшенный член того же
   порядка, что удержанные. Выражение `∂τ_rz/∂r + τ_rz/r`, которое считает код,
   осевым равновесием не является. Называть его так — вторая ошибка поверх
   первой.

**Сделано.** `docs/physics_equations.md` приведён к тому, что реально считается,
с таблицей оценок; в `publication/code/physics_check.py` второе выражение
называется `shear_balance_residual` и снабжено `EQUILIBRIUM_R2_STATUS`.

**Осталось (📝).** Решение Дёмина, какой вариант идёт в статью:

* минимальный — оставить только радиальное уравнение как физический лосс,
  честно описать редукцию и привести таблицу выше; сдвиговый член назвать
  регуляризатором гладкости, а не уравнением равновесия;
* полный — ввести `z_norm` во вход сети и считать обе формы (инфраструктура
  частично есть: флаг `use_z` в `models/strain/pinn_total_strain_system.py`).

Объём работ различается на порядок; данные для полного варианта есть — `z`
присутствует в каждом `.rpt`.

## E-03 🟡 Фантомный уровень `μ = 0.002`

В таблице параметров рукописи четыре уровня трения: 0.002, 0.025, 0.05, 0.10.
Во всём массиве `μ = 0.002` встречается **ровно один раз** (`audit/RAW_AUDIT.md`,
§4). Источник — единственный файл
`Vel_40/aisi_1020_2a_40_rd_500_cal_100_v_40_fric_02.rpt`, где токен `fric_02`
вместо `fric_025`; правило разбора `rpt_utils.f()` (снять первый символ,
поделить на 1000) превращает опечатку в уровень фактора.

**Следствия:** уровней три; полное декартово произведение
`7 × 6 × 5 × 3 × 5 = 3150`; этот набор отбраковывается.

**Сделано.** `filters.MU_GRID`, флаг `drop_mu_offgrid`; `docs/data_format.md`
и `data/README.md` исправлены. **Осталось (📝):** таблица параметров в `.tex`.

## E-04 🔴 `PLANE_ORDER` не согласован с коммитнутым препроцессингом

`preprocessing_res_np` укладывает компоненты в порядке
`(σ_rr, σ_θθ, σ_zz, τ_rz)` — проверено на сырых `.rpt`: traction-free
выполняется в слотах 0 и 3 **до** всякой перестановки (`audit/RAW_AUDIT.md`, §3).
Поверх такого стека `PLANE_ORDER = [2, 0, 1, 3]` даёт
`(σ_zz, σ_rr, σ_θθ, τ_rz)` — перепутанные компоненты.

Обратная подстановка из чисел проверки в `docs/data_format.md`
(`σ_rr ≈ 5.9`, `σ_θθ ≈ 144`, `σ_zz ≈ 99`, `τ_rz ≈ 3.8` МПа) даёт, что в `.pkl`
плоскости лежат как `(σ_θθ, σ_zz, σ_rr, τ_rz)`. То есть `.pkl` собран **другой
версией скрипта**, чем та, что опубликована. Обученные модели, скорее всего, в
порядке — сломана воспроизводимость: рецензент, запустивший репозиторий от
сырых данных, получит перепутанные компоненты.

**Проверка на настоящем `.pkl` в одну строку:** `σ_rr` — та плоскость `p`, у
которой `mean|y_stress[p][:, -1]|` минимален.

**Осталось (🔶).** Выбрать одно: пересобрать `.pkl` текущим препроцессингом и
поставить `PLANE_ORDER = [0, 1, 2, 3]`, либо привести порядок стека в
`preprocessing_res_np` к тому, каким собран `.pkl`. Второе дешевле, первое
честнее. В любом случае прогнать проверку выше и записать её результат в
статью — это дешёвая страховка от повторного замечания.

## E-05 🟡 Протокол обучения из §2.2.4 рукописи в коде отсутствует

Рукопись: «Adam followed by LBFGS for the DeepXDE backend, Adam with cosine
schedule for the others». В репозитории:

* `cosine` / `lr_scheduler` не встречается **нигде** (проверено `grep` по
  `models/`, `launch/`);
* `LBFGS` есть только в `vpinn_model.py::_make_opt`, но в пространство поиска
  Optuna не входит — категориальный выбор везде `["Adam", "AdamW"]`;
* пространства поиска, `N_TRIALS = 20`, `SPLIT_SEED = 42`, `n_splits = 3` во
  всех шести ноутбуках уже совпадают.

То есть **сравнение было менее «неконтролируемым», чем описано в статье**, и
претензия Reviewer #1 в этой части адресована тексту, а не эксперименту. Это
хорошая новость, но пользоваться ею можно только после того, как текст приведён
к коду.

**Сделано.** `publication/code/protocol.py` — единый источник протокола,
`suggest_config()` для всех шести моделей, `assert_conforms()` (падает на LBFGS
и на cosine), таблица `PROTOCOL_DIFF` «что было / что стало» для статьи.

## E-06 🟡 μ-дубликаты: 13.2 %, а не «~31 %»

323 набора из 2443 (13.2 %) имеют побитово тот же профиль, что другой набор,
отличающийся только меткой μ; 159 комбинаций `(Q, k, α, v)` схлопываются
полностью. Проверено сильнее: у 40 из 40 случайно выбранных пар совпадают **все
числовые данные `.rpt`**, различаются только заголовки (путь к `.odb`, отметка
времени).

Формулировать это надо точно: это не «трение не повлияло на решение», а **один
расчёт, размноженный под несколькими метками трения**. Разница существенная —
первое было бы физическим наблюдением, второе есть дефект генерации данных.

Цифра «~31 %» из `docs/data_format.md` не воспроизводится ни на каком порядке
фильтров и вычищена. **Сделано:** `filters.find_mu_duplicates`, доки.

## E-07 🔴 Hold-out мерит интерполяцию

Случайные 15 % из того же пространства параметров показывают интерполяцию
(Reviewer #1). **Сделано:** `publication/code/splits.py` — вынос целых граней
факторного пространства, `matched:<region>` того же объёма как контроль,
`<axis>_mid` как внутренний срез, `coverage_report()` с колонкой «вне
диапазона?». Заявлять обобщение можно только по осям, где стоит «ДА».
**Осталось (🔶):** прогон. Подробности в `PLAYBOOK.md`, §7.

## E-08 🔴 Один прогон; ранговый тест по наборам

Разница 0.4–0.8 МПа между бэкендами получена из одного прогона на модель, а
Friedman–Nemenyi считался по 304 наборам hold-out — это разброс выборки, а не
разброс обучения. **Сделано:** `publication/code/stats.py` — блоками теста
служат сиды; `friedman_test` отказывается работать при `n_blocks < 3` с прямой
отсылкой к этой претензии; `significance_gate()` помечает «не установлено» всё,
что меньше межсидового σ; `pool_guard()` запрещает объединять сплиты.
**Осталось (🔶):** прогон на 5 сидах.

## E-09 🔴 FEM не валидирован экспериментально ⛔

Экспериментальных данных нет и не будет. Пункт непреодолим имеющимися
средствами; лечится только переопределением вклада — см. `PLAYBOOK.md`, §4.
Любая формулировка, намекающая на соответствие физическому процессу, будет
отклонена повторно.

## E-10 🔴 Нет методической новизны ⛔

«A difference in autodiff backend is a software choice, not a modelling
contribution» (Reviewer #2). Сравнение бэкендов должно уйти из заголовочных
выводов в раздел воспроизводимости. Новая рамка вклада — `PLAYBOOK.md`, §4.

## E-11 🟡 «~8 наборов с Q = 0.25»

Восемь — это сколько их попало в конкретную holdout-выборку. Всего на сырой
сетке их **275**. **Сделано:** доки исправлены; фильтр снимает уровень целиком.

## E-12 🟡 Несбалансированность по `v`

`v = 5` м/мин — 122 набора против 554–607 у остальных уровней. Практическое
следствие: при подвыборках для кривых обучения (пункт 5) уровень может исчезнуть
целиком. **Сделано:** `learning_curves.stratified_nested_subsamples`.
**Осталось (📝):** оговорка в описании эксперимента.

## E-13 🔴 Библиография не собирается 📝

В PDF нет ссылок и списка литературы; 56 warnings «Citation … undefined» при
нуле ошибок, поэтому незаметно. Причины: `\bibliographystyle` закомментирован;
`\bibliography{cas-refs.bib}` — с расширением, должно быть без; `cas-refs.bbl`
содержит копию `.bib` (`\bibitem` — ноль штук). Плюс остаток biblatex
`%\printbibliography`. Чинится за 15 минут, но статья сейчас физически без
ссылок — делать первым.

## E-14 🔴 Дубль списка компонент в Conclusions 📝

Напечатано, что и PINN (JAX/Flax), и VPINN лидируют по одной и той же тройке
`σ_rr, σ_zz, τ_rz`. По телу статьи и по таблице рангов ВКР правильно: **VPINN
лидирует по `σ_θθ`**. Замена:
`Per-component leadership is split: PINN (JAX/Flax) is best on σ_rr, σ_zz and
τ_rz, while VPINN leads on σ_θθ.`

## E-15 🟡 Аннотация рассинхронизирована 📝

* «best macro-**mean squared error** of 11.51 MPa» — это RMSE (размерность МПа);
* «a group of three models that were not statistically distinguishable from one
  another … forming a single cluster» — тело статьи уже исправлено, аннотация нет.
  При CD = 0.432 и рангах (JAX 2.84, VPINN 3.06, PyTorch 3.31) соседние пары
  неразличимы (Δ = 0.22 и 0.25), а крайняя пара JAX ↔ PyTorch (Δ = 0.47 > CD)
  **различима**: тройка связана только через VPINN. Терминология «two cliques» /
  «upper group» гуляет между L388 и L390 — свести к одной.

Формально это свойство теста Немени: неразличимость не транзитивна.
`stats.NemenyiResult.to_text()` печатает про это предупреждение, чтобы ошибка
не воспроизвелась в новой версии.

## E-16 🟢 Косметика `.tex` 📝

`\ead{Georgii.v.romanenko@gmail.com}` — привести к строчным; CRediT
«Software,Formal Analysis» без пробелов, пробел перед точкой, разнобой
дефис / тире; отсутствуют поля `pages` у `muransky2026application`,
`watanabe2023tree`, `shi2006analytical`; `shi2006analytical` — статья про
orthogonal cutting, цитируется как источник параметров Джонсона–Кука для
AISI 1020, релевантность проверить; 16 underfull hbox/vbox; PNG тяжёлые
(~0.5 МБ каждый), сборка близка к лимиту free-плана Overleaf.

## E-17 🟡 `PLANE_ORDER_STRAIN` в `training/data_pipeline.py`

`training/data_pipeline.py` объявляет `PLANE_ORDER_STRAIN = [1, 0, 2, 3]` и
использует его в `build_strain_dataset`, тогда как `docs/data_format.md` и
`models/strain/*.py` работают с `[2, 0, 1, 3]`. Деформации в статье не
используются, поэтому на её выводы это не влияет, но при переходе к работе по
деформациям (задел) расхождение выстрелит. Зафиксировано здесь; чинить вместе
с E-04, потому что диагностика та же.
