version: stress_mlp_v1

model:
  type: mlp
  in_dim: 6
  out_dim: 4
  hidden: [256, 256, 256, 256, 256]
  activation: tanh

training:
  epochs: 5000
  batch_size: 1024
  lr: 1.0e-3
  patience: 80

data:
  holdout_frac: 0.15
  seed: 42
  n_folds: 5
